name: Prod Deploy
on:
  push:
    branches:
      - main

jobs:
  build:
    uses: LamArt/open-schools-platform/.github/workflows/nextjs.yml@dev
    secrets:
  #      GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
  #      GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_API_KEY }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v3
      - name: Dev deploy open-schools-platform
        uses: D3rHase/ssh-command-action@v0.2.2
        with:
          host: ${{secrets.DEV_HOST}}
          user: "root"
          private_key: ${{secrets.DEV_PRIVATE_KEY}}
          command: |
            echo '--- START WORK ON REMOTE SERVER ---';
            set -e;
            
            echo '--- CD ---';
            cd /opt/open-schools-platform-frontend;
            
            echo '--- CHECKOUT ON BRANCH ---';
            git checkout main;
            
            echo '--- Upgrade repo ---';
            git pull;                    
            
            echo '--- Create prod image ---';
            docker build -f docker/prod.Dockerfile -t nextjs-prod .;
            
            echo '--- Force updating services ---';
            sh force-update-services.sh "prod";
            
            echo '--- Success! ---';