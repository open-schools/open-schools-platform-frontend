export enum QueryStatuses {
    SENT = 'SENT',
    ACCEPTED = 'ACCEPTED',
    IN_PROGRESS = 'IN_PROGRESS',
    DECLINED = 'DECLINED',
    CANCELED = 'CANCELED',
}

export enum TicketStatuses {
    NEW = 'NEW',
    OPENED = 'OPENED',
    CLOSED = 'CLOSED',
}
