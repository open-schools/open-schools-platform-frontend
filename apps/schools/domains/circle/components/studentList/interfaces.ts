export interface RowType {
    id?: string
    name: string
    address: string
    accepted_count: number
}

export interface TableType {
    id?: string
    name: string
    address: string
    accepted_count: number
}
